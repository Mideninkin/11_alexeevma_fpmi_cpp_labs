#include <iostream>
#include <random> 
#include <ctime>

int main() 
{
const int max = 100;
int n, m, arr[max][max];
int min = 1;
while (!(std::cin >> ) || (d < min) || (d > max))
    {
        std::cout << "Invalid input" << '\n';
        std::cin.clear();
        std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');
    }







    
    
    
    
    
    
    
    
    
    
    












    return 0;
}