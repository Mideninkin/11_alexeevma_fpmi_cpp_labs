#include <iostream>
#include <cmath>

void SortBubble (int[a], int size)
    {
    for (i = 0; i < n; i++)
    {
        for (j = 0 ; j < n - i - 1; j++)
        {
        if (abs(a[j]) > abs(a[j + 1]))
        void Swap (int *a[i], int *a[j])
        {
            m = a[i];
            a[i] = a[j];
            a[j] = m;
        }
        } 
    }
    }

main()
{ 
    SortBubble ()

    return 0;
}
  
